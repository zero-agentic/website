# implementation-notes

Zero Agentic 官网建站过程笔记。记录偏离计划的决定与待用户确认项。

## 已确认的前提（来自用户）

| 项 | 结论 |
| --- | --- |
| 站点性质 | 公司对外官方门户，**不是**产品服务站点 |
| 展示作品 | 明确否决，不做作品画廊 |
| 技术栈 | Astro + Tailwind，静态构建 |
| 部署 | GitHub Pages，仓库 `zero-agentic/website`，自定义域名 `zero-agentic.com` |
| 语言 | 英文为主（`/`）+ 中文（`/zh`） |
| 页面 | 首页、产品、关于、联系、隐私政策、服务条款、博客 |
| 产品线署名 | **仅 DesignDance**，状态为「开发中，尚未上线」 |
| 视觉方向 | **LATENT FIELD** —— 实时演算的潜空间场（重构后。首版 SPEC SHEET 已作废） |
| 明暗 | 完整切换，深色为默认 |
| 视觉素材 | 允许使用（业务方澄清：否决的是「作品画廊」而非「页面视觉」） |

## 视觉重构（SPEC SHEET → LATENT FIELD）

首版按 SPEC SHEET（半导体数据手册）方向建成后，业务方判定「太丑」，四项具体理由：气质不对（不像创意公司）、太素几乎无色、像表格不像网站、没有图像空荡荒凉。

按 impeccable 的 redesign 规则处理 —— **替换视觉世界，不在旧世界上打磨**：产品事实、双语内容、以及此前修掉的全部工程问题保留；DESIGN.md 整体替换。

### 关键发现：品牌资产一直没被用上

公司标识 `zero/packages/design/src/icons/logo.svg` 的底色是 `#05050C`（近黑），四颗渐变球是橙 `#F9861F` / 蓝 `#33ADEC` / 紫 `#AE87EF` / 粉 `#E692BA`。

**标识本来就是为深色设计的，且自带一套饱和色板。** 首版只用了 `#4bb1d4` —— 那是 DesignDance 的 UI 主色，不是公司标识色 —— 这是「太素」的根源。

### 方向选择：真实原型，不是线框

首版的失败教训是：ASCII 线框让业务方选中了一个实际观感他并不喜欢的方向。因此这次做了**三个可运行的 HTML 原型**（`prototypes/`），带真实动效，业务方看实物后选定 P1 · LATENT FIELD。

| 原型 | 方向 | 结果 |
| --- | --- | --- |
| P1 | 实时演算的潜空间场（WebGL） | **选中** |
| P2 | logo 球体放大成场景主体 | 备选 |
| P3 | 满幅色带分割 | 备选 |

同时确认了一处此前的误读：业务方否决的是「作品画廊」，不是「页面有没有视觉设计」。这两件事被首版混为一谈。

### 无图像生成能力这个约束，指向了更好的答案

本环境没有图像生成工具（只有 cwebp/sips/magick 这类转换器）。因此「大图主导」不能靠贴图实现，只能程序化生成 —— 而对一家做生成式模型的公司，**首屏视觉当场算出来**恰恰是概念与形式的统一，比任何贴图都更贴题。

## 设计决策

### 1. 明暗双模式（重构后）

深色为默认（标识画在 `#05050C` 上），浅色为对等实现，用户可切换。首帧之前由同步内联脚本定色，避免 FOUC。详见 DESIGN.md。

### 2. 色板校验口径

每个前景色对**三种底色**（主背景、抬升面、页脚沉底）逐一校验，取最小值。只验证主背景会漏掉页脚 —— 浅色的 `--fg-3` 就是这样卡在 4.49:1 被 Lighthouse 抓到，随后从 `#6f6b63` 改为 `#68645c`。

### 3. 字体

- 标题：**Bricolage Grotesque Variable**（自托管），带宽度轴与光学尺寸轴。标题层次靠宽度轴做，不靠再引入一个颜色。
- 正文：**Manrope Variable**（自托管）。
- 中文：回落系统面。中文无合理的自托管可变字方案，子集化会显著拖慢首屏。

### 4. 内容架构：结构化数据 + 共享组件

页面内容不走 i18n key-value 字典，而是 `src/content/site/{en,zh}.ts` 两份结构化数据，由同一套组件渲染。原因：门户站的文案是成段的叙述，字典化会把它切碎成机械翻译；两份独立数据允许中英各自说人话。

## 偏离与待确认

### 偏离：标识不复用 `.bead`

色点与标识共用 CSS 变量里的四色，但标识本身仍是交叠的 SVG。原稿球心距小于直径，四颗球叠在一起；用 CSS grid 并排会把标识变成四颗分离的点。色是同一份，形状不是。

### 偏离：场的能量与 veil 绑在同一个 `variant` 上

原先 `intensity` 只管着色器能量，veil 由每个页面自己铺一层。能量衰减和压暗形状其实是同一件事——文字落在场的哪一带——拆开就会漏铺 veil（404 曾经就是）。现在 `hero` / `page` / `band` 同时决定这两件事，调用方不再另铺。

### 偏离：impeccable 的 concept-seed 脚本不可用

`concept-seed.mjs`（v4.1.1）在本机静默退出、零输出，禁用沙箱后同样。已按 skill 的降级路径执行：自行完成七候选推导，用 `/dev/urandom` 作外部随机源掷骰分配方向（结果 #1，恰为推导排序第一），四张牌通过结构化提问交付用户选择。用户选定 A · SPEC SHEET。

## 检查轮次与修复

### 首版（SPEC SHEET）的检查记录

以下轮次针对已作废的数据手册版本，保留作为工程问题的处理记录 —— 其中的**非视觉**修复（h1 层级、尾斜杠、中文串、法务条款、JSON-LD）全部延续到了重构版本。

**第一轮发现并修复：**

| 问题 | 处理 |
| --- | --- |
| body 背景测量网格与规则线系统打架，横切 wordmark 与参数表，视觉发脏 | **移除**。规则线、表格线、尺寸线已构成完整的线语言，背景网格是多余的第二套 |
| `SectionRail` 未激活时就渲染出一段孤立的青蓝横杠 | 默认 `opacity-0`，`data-rail-active` 后淡入 |
| 中文 label 的 `letter-spacing: 0.14em`（英文标注值）把 CJK 字形拉散 | `html:lang(zh)` 下降到 `0.06em` |
| `--font-mono` 无 CJK 回落，中文落进系统等宽的宋体 | 回落链加入 PingFang SC / 冬青黑 / 微软雅黑 |
| 产品页与关于页的三列条目中间列过宽，标题与描述之间大片空白 | 收紧到 `3.5rem_minmax(0,13rem)_1fr` |

**第二轮发现并修复：**

| 问题 | 处理 |
| --- | --- |
| 真实 390px 视口下导航换行，中文项被压成竖排 | wordmark 文字 `sm` 以下隐藏只留符号，所有导航项 `whitespace-nowrap`，间距分档 |
| 标识栏在 390px 下折成两行 | `DOC. NO.` 与 `§` 段在 `sm` 以下隐藏，保留实体名 + REV |
| **404 页语言切换指向 `/zh/404`，该路径不存在**（链接检查发现，肉眼不可见） | `BaseLayout` 增加 `altHref` 与 `noindex` 两个 prop；404 切换指向 `/zh`，并跳过 canonical/hreflang 改为 noindex |

**第三轮（独立终审后）修复：**

独立终审以 `fix` 结论返回 7 条实质问题，全部处理：

| 问题 | 处理 |
| --- | --- |
| 4 类页面（产品/关于/联系/博客索引）**没有 `h1`**，大纲从 h2 起头。Lighthouse 只查降序不查 h1 存在性，故此前未被发现 | `SectionHead` 增加 `as` prop；4 个页面根传 `h1`；页脚分组标签从 `h2` 降为 `p`（`nav` 已有 `aria-label`）。17/17 页各恰好 1 个 h1 |
| 中文页残留 5 处硬编码英文串（`Pending legal review`、`aria-label="Footnote N"`、`7 ITEMS` 等），中文读屏用户会听到英文 | `ISiteContent` 增加 `ui` 段；`SpecTable` 与 `LegalPage` 经 `getLocaleFromUrl` 取语言 |
| 联系页文案引用的 `§5`、`§6` **在站内不存在**，且编号出现 4→7 断档 | `ILegalDoc` 增加 `n` 字段，隐私政策 = §5、服务条款 = §6，并接入标识栏 |
| `trailingSlash: 'never'` 与默认的 `format: 'directory'` **不一致**，GitHub Pages 上每条站内链接吃一次 301，canonical 指向立刻重定向的 URL | 改为 `trailingSlash: 'always'` + `format: 'directory'`；`localizePath` 输出尾斜杠，新增 `normalizePath` 供当前页判定；内容层 href 同步。（先试过 `format: 'file'`，Astro 会把 `/zh` 重写成不存在的 `/zh/index.html`，遂放弃） |
| 法务稿缺 EU AI Act 第 50 条、CCPA 不出售声明、泄露通知、GDPR 第 13 条要素、DMCA 代理人、争议解决与一般条款 | 隐私政策扩至 17 条、服务条款扩至 16 条，中英同步。仍为工程稿 |
| `礼制肖像` 不是中文词（礼制指社会礼法制度） | 改为 `礼服肖像` |
| 联系邮箱作为确定事实渲染在 7 处，而站内其他 9 项未确认事实都是 TBD | **未转为 TBD**：联系页与两份法务文档的数据请求条款都指向它，写成 TBD 会让这些页面失去功能。改为集中到 `contact-email.ts` 单一常量并注明未确认，同时列为交付清单第一项交用户决定 |

同轮处理的次要项：移除粘性页头的 `backdrop-blur`（与"纸不发光"的世界主张冲突）；Unicode 箭头换成 `Arrow.astro` 绘制 SVG；英文直引号转弯引号；小屏恢复联系入口；`TBD` 改用 `abbr` + 视觉隐藏说明，使读屏无需 hover 即可获知含义；博客文章在另一语言缺失时不再发 hreflang。

未处理：`og:image` 缺失（分享卡片为空白，但无技术故障，留待业务方决定视觉素材）。

**终审 verdict pass 另发现一处两轮都漏掉的既有 bug：** `BaseLayout` 用 `<set:html value={…} />` 输出 Organization JSON-LD —— Astro 把它解析成一个名为 `set` 的未知元素，结构化数据变成转义属性，且在 `<head>` 里会隐式终止 head 解析。**该结构化数据从未在任何页面渲染过。** `astro check` 看不到（是模板不是类型），Lighthouse SEO 不强制结构化数据，因而躲过每一道闸。已改为 `<script type="application/ld+json" is:inline set:html={…} />`，并验证可 `JSON.parse`。

### 重构版（LATENT FIELD）的检查与修复

| 问题 | 处理 |
| --- | --- |
| 首屏默认跟随系统偏好，Chrome 默认浅色 → 大多数访客看到的不是品牌本色 | 深色改为默认，仅在用户显式切换后记忆 |
| 浅色模式下场被 `--field-opacity: 0.5` 压得几乎无色 | 提到 `0.82`，浅色成为「纸上的水彩」而非被漂白的深色版 |
| **`.bead` 无 `display`**，inline span 的宽高无效 —— 产品页的品牌圆珠根本没渲染（首页在 flex 容器里侥幸生效） | `display: block` |
| 规格表 `dt`/`dd` 各自的下边框被列间距切断，分隔线不连续 | 改用 HTML5 允许的 `dl > div > (dt,dd)` 结构包裹每行 |
| 品类网格用 `gap-px` + 容器底色，末行空缺处露出灰块 | 改为每格自带 `border-b border-r` |
| 首页联系区邮箱是 inline，后面的链接挤在同一行并重叠 | 邮箱改为块级 |
| 真实 390px 下导航溢出，语言与主题按钮被推出屏幕 | `sm` 以下收进汉堡菜单（含 Escape 关闭、断点变化自动收起） |
| 内容里残留旧世界的 `§N` 章节引用，与新的 01/02/03 编号不一致 | 全部统一 |
| 若干无效的 `font-500` 类 | 改为 `font-medium` |
| **浅色 `--fg-3` 在页脚沉底上只有 4.49:1**（此前只验证了主背景，漏掉页脚） | `#6f6b63` → `#68645c`；校验口径改为「对三种底色取最小值」 |

### 第四轮：文案与排印（业务方反馈）

业务方指出三点：文案要面向用户、大部分文案需深度优化、首页文字太大。另外要求页头透明、与背景融为一体。

| 问题 | 处理 |
| --- | --- |
| 文案是工程师视角（「合规格交付物」「那一层工具」「平台要求的清单文件」「异步任务内核」） | 全站文案两轮重写，改成说结果而非说实现。首屏从抽象的「图之后的那一段」改为「AI 给你一张图，我们给你能用的文件。」；描述改为具体可感的「Logo 放大到招牌上不糊，应用图标一次给齐商店要的每个尺寸，老照片修完还能拿去印」 |
| 首页文字过大（1440px 下标题到 96px），中文断行把「一张图」劈成两半 | 标题刻度整体降档；**中文单独再降一档并把字距归零**（汉字字面率高，`-0.035em` 是为拉丁字母调的）；hero 改为 lead/trail 各占一行，断行完全可控 |
| 中文 h1 里有一个字面空格（`{' '}` 分隔符） | 随 hero 分行一并消除 |
| 数据手册残留：`ZA-C1` 型号、`ZA-P1`、「尺寸阶梯」、页脚 `Rev. 2026.09` | 全部移除，相应字段从内容契约中删除 |
| 页头有一块独立的背景色块，与场之间有硬分界 | **根因是 `sticky`**：页头在文档流里占一行，场只能从它下面开始，那一行 body 底色就是那块色块。改 `fixed` 脱离文档流，页头零背景，场从页面最顶端铺满 |
| 页头实底伪元素盖住导航内容 | `::after` 在 DOM 顺序上排在内容之后，内容需显式 `z-index: 1` |
| 页头哨兵 1px 高 + `rootMargin: -8px` **永远无法相交**，`data-stuck` 恒为真，透明态从未生效 | 哨兵改为 72px 高、`threshold: 0` |
| 页头透明后导航文字压在流动的场上 | 压暗并入各区段 veil 顶端（连续渐变，不形成色块），按四个品牌色算出 68% 为安全值：最坏 5.88:1，55% 会掉到 4.01:1 |

## 验证结果

| 项 | 结果 |
| --- | --- |
| `astro check` + 构建 | 0 errors / 0 warnings / 0 hints，17 页，产物 652 KB |
| WCAG 对比度 | 全部前景色对**三种底色**逐一校验，两模式最小值 5.45:1 / 4.60:1 |
| Lighthouse（桌面 navigation） | **深色与浅色各跑一次**，均 100 / 100 / 100 / 100，52 项通过 0 失败 |
| 透明页头下的导航对比度 | 按四个品牌色逐一计算，最坏 5.88:1（深色 / 次要文字 / 粉色流带） |
| 工程师词汇残留 | 全站扫描零命中 |
| impeccable detector | 零发现 |
| 内部链接 | 352 条 / 17 页，零失效，尾斜杠一致（无 301 跳转） |
| 标题层级 | 17/17 页各恰好一个 `h1` |
| 中文页英文串残留 | 零 |
| JSON-LD | 真实渲染且可 `JSON.parse` |
| 移动端 390px | 无横向溢出，导航收进汉堡菜单 |
| 方向契约存活于构建产物 | `grep "DIRECTION CONTRACT" dist/index.html` 命中 |

### 待用户提供的真实事实

以下均已在页面上以显式占位标记呈现，**未编造**：

- 公司成立年份、注册州、办公地址
- 联系邮箱（当前用 `hello@zero-agentic.com` 作占位，集中定义在 `src/content/site/contact-email.ts`）
- 社交 / GitHub 组织链接
- 团队信息
- 法务页中的司法管辖区、数据留存期限、争议解决条款

### 待确认：法务页需律师审阅

隐私政策与服务条款已按 AI 图像/视频生成服务的实际风险面撰写（用户上传内容、生物特征信息、生成内容权属、模型训练用途），但这是工程稿，不是法律意见。上线前须由执业律师审阅，特别是 CCPA/GDPR 适用性与生物特征信息条款（伊利诺伊 BIPA、德州 CUBI 对人脸数据有专门规定，而婚纱照/宝宝照品类直接触及）。

## SEO 与 GEO 优化（2026-09-06）

### 关键词与内容边界

- 英文核心意图：`AI image and video generation company`、`AI design generator`。
- 中文核心意图：`AI 图片与视频生成公司`、`AI 设计生成工具`。
- 产品长尾词只使用页面已有的计划范围：Logo、应用图标、海报、游戏素材、老照片修复、肖像。
- 标题、描述与可见正文同步改写；没有添加 Google 明确忽略的 `meta keywords`。
- 没有添加 `llms.txt`：当前没有主流答案引擎的官方统一消费规范，现有 sitemap、静态 HTML 与开放抓取已覆盖可发现性。

### 结构化数据

`BaseLayout` 统一输出以稳定 `@id` 相连的 `Organization`、`WebSite` 与页面实体；内页按类型补充 `AboutPage`、`ContactPage`、`CollectionPage`、`Product`、`BlogPosting` 与 `BreadcrumbList`。DesignDance 没有 `Offer`，因为产品尚未上线且价格未定；结构化数据只描述页面上可见的事实，不伪造可购买状态。

组织实体新增 512 × 512 可抓取标识 `public/logo.svg`。文章的发布日期、修订日期、作者与发布者均来自现有内容数据。

### 验证

- `pnpm build`：17 个页面生成成功。
- 逐页解析 17 个 HTML：16 个可索引页面均有 canonical 与可解析 JSON-LD，页面实体 URL 与 canonical 一致；404 无 canonical、无 JSON-LD。
- 中英文产品页分别包含 Product 与本地化 BreadcrumbList；中英文文章页分别包含 BlogPosting、发布日期与本地化面包屑。
