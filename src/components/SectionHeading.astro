---
interface Props {
  title: string
  as?: 'h1' | 'h2'
  class?: string
}

const { title, as: Tag = 'h2', class: className }: Props = Astro.props
---

<Tag class:list={['title-lg', className]}>{title}</Tag>
